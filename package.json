{
  "dependencies": {
    "eslint": "^8.43.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.8"
  },
  "scripts": {
    "build": "tsc",
    "format": "prettier --write",
    "format:check": "prettier --check",
    "lint": "eslint --max-warnings=0 src",
    "type-check": "tsc --noEmit",
    "deploy": "git add -A && git commit -m \"quick commit\" && git push"
  }
}
